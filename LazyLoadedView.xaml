<?xml version="1.0" encoding="utf-8"?>

<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="LazyViewBlankPageIssue.LazyLoadedView">
  <Border Stroke="#1D1D1F05"
          BackgroundColor="White"
          Margin="16,16,16,16"
          Padding="16,8,16,16">
      <Border.StrokeShape>
        <RoundRectangle CornerRadius="8,8,8,8" />
      </Border.StrokeShape>
      <StackLayout>
          <Grid RowDefinitions="*,*">
              <Label Text="Test 1"/>
              <Label Text="Test 2" Grid.Row="1"/>
          </Grid>
      </StackLayout>
  </Border>
</ContentView>